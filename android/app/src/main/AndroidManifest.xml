<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Runs the foreground service that keeps monitoring alive while the
         driver is working (spec section 25). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <!-- A real device proved Android blocks BoltJobNotificationListenerService's
         attempt to fire Bolt's own suppressed fullScreenIntent while backgrounded
         unless JobFilter holds this — confirmed directly in the platform's own
         "Background activity launch blocked" log, which named
         BAL_ALLOW_SAW_PERMISSION as exactly what would lift the block. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>

    <!-- Android 11+ package visibility: lets JobFilter recognize the two
         supported driver apps (e.g. to show "not installed" states) without
         being able to see the rest of the device's installed apps. -->
    <queries>
        <package android:name="com.ubercab.driver"/>
        <package android:name="ee.mtakso.driver"/>
    </queries>

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data android:name="flutterEmbedding" android:value="2"/>

        <!-- Spec section 14: the Accessibility Service, scoped to the two
             supported driver apps via accessibility_service_config.xml. -->
        <service
            android:name=".automation.JobAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService"/>
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config"/>
        </service>

        <!-- Spec section 25: foreground service with a persistent notification
             while automation is active. -->
        <service
            android:name=".automation.AutomationForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Monitors supported driver apps to evaluate incoming job offers against driver-defined rules"/>
        </service>

        <!-- Fires Bolt's own suppressed full-screen job-offer intent when
             Bolt is backgrounded, so the Accessibility Service pipeline gets
             a real card to read — see BoltJobNotificationListenerService. -->
        <service
            android:name=".automation.BoltJobNotificationListenerService"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService"/>
            </intent-filter>
        </service>
    </application>
</manifest>
